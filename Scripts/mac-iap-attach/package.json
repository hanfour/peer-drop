{
  "name": "peerdrop-mac-iap-attach",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright script for attaching Mac App Store tip-jar IAPs to the inflight v6.0.0 version via the ASC web UI (no API path exists).",
  "scripts": {
    "attach": "tsx iap-attach-mac.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "playwright": "^1.50.0",
    "tsx": "^4.7.0"
  }
}
