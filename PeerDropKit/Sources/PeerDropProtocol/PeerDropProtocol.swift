/// Placeholder for the PeerDropProtocol module.
///
/// In M1d, this module will own the wire format, message envelope,
/// and version negotiation (incl. v5.4 PR7 peerProtocolVersion).
///
/// Until M1d migrates the source files, this empty enum exists only
/// so `swift build` has something to compile.
public enum PeerDropProtocol {}
