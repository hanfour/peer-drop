/// Placeholder for the PeerDropSecurity module.
///
/// In M1d, this module will own PeerIdentity, DeviceIdentity,
/// ChatDataEncryptor, TrustedContact, Double Ratchet, SAS, relay
/// crypto (v5.4 PR1–PR8).
///
/// Until M1d migrates the source files, this empty enum exists only
/// so `swift build` has something to compile.
public enum PeerDropSecurity {}
